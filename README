SQL - beg-SQL 

Files here:

1) SQL Notepad                  - Chronological timeline of T-SQL Musings

2) adricBeginTry                - Update statement using error catch, LEN & REPLACE functions, and UPDATE from JOIN statement

3) adricInsertUsingGUI          - SSMS GUI INTO script

4) adricLatestEnroll            - (Select statement using MAX,CASE)

5) adricMERGE                   - Merge statement to sync destination from source (update when MATCHED, insert when not)

6) adricManualQ                 - Mass queue up award letters for students (user defind TALLY table here to make things                                          faster)

7) adricPartitionBy             - Practicing ROW_NUMBER() over Partion By

8) adricUnionSubQuery           - Had small # of students I need to insert records into a table with, and instead of punching                                     in values one-by-one into insert DML statements, I wanted to format T-SQL to spit out                                        insert statements for me

9) adricUnionSubQuery_2         - Wrapped my insert statements with error checking: @@TRANCOUNT,BEGIN TRY, CATCH, XACT_STATE                                     (), ERROR_NUMBER(), ERROR_MESSAGE(), RAISERROR, END CATCH, END TRY

10) adricUpdateErrorCatch       - (T-SQL batch returns results when bit = 0, commits change when bit = 1, uses BEGIN TRY,                                        ERROR catch)

11) complexUpdatev1             - (Update statement using BEGIN TRY,CATCH, OBJECT_ID,DROP Temp TABLE, Variable @Bit to test)

12) simpleDlrUpPrvntMistakes    - (Practicing different style for updating, will show before change / after change, and                                         rollsback data before I must manually uncomment COMMIT)

13) sproc_gatherAUTOMATEDinfo   - User created sproc that gathers and present import FAA info quickly for analyst troublesho                                     oting

14) sqlLoop                     - Loop using WHILE, printing 1-10


